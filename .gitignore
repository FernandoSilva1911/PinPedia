node_modules/
*.log
.impeccable/

# pasta de trabalho da Vercel (contem ids do projeto e token OIDC)
.vercel/

# segredos: .env, .env.local, .env.bak ...
# a excecao precisa vir DEPOIS da regra, senao nao vale
.env
.env.*
!.env.example

# material de referencia de terceiros, nao redistribuir
*.pdf

# versao antiga do DERS, mantida so localmente
FernandoSilva_relatorio_DERS.docx
